import 'package:flutter/material.dart';

class TGColors {
  // Primary colors
  static const Color primary = Color(0xFF2E86AB);  // A pleasant travel blue
  static const Color secondary = Color(0xFFF18F01); // Warm accent color
  
  // Background colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF8F9FA);
  
  // Text colors
  static const Color textPrimary = Color(0xFF212529);
  static const Color textSecondary = Color(0xFF6C757D);
  static const Color subtext = Color(0xFF6C757D);  // Same as textSecondary for consistency
  
  // Utility colors
  static const Color error = Color(0xFFDC3545);
  static const Color success = Color(0xFF28A745);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF17A2B8);
  
  // Additional colors
  static const Color border = Color(0xFFDEE2E6);
  static const Color disabled = Color(0xFFE9ECEF);
}
